import React from "react";
import { Header, Info, Product } from "../../components";

const Homepage = () => {
  return (
    <div>
      <Header />
      <Info />
      <Product />
    </div>
  );
};

export default Homepage;
